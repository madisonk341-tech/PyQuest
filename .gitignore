# Backend (Python/Flask)
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/instance/
*.pyc

# Frontend (Node/Vite)
frontend/node_modules/
frontend/public/pyodide/
frontend/.vite/

# frontend/dist/ is intentionally committed: PythonAnywhere's free tier has
# no usable Node.js, so we build locally and ship the built output instead.

# Editors / OS
.DS_Store
Thumbs.db
